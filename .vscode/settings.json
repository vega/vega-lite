// Place your settings in this file to overwrite default and user settings.
{
  "editor.tabSize": 2,
  "files.exclude": {
    "**/*.js": {"when": "$(basename).ts"},
    "**/*.d.ts": {"when": "$(basename).ts"},
    "**/*.js.map": true,
    "coverage/**/*": true,
    "_site/**/*": true
  }
}
